
# Pattern: Frigate config to consume RTSP via Socat relay
cameras:
  site_a_cam1:
    ffmpeg:
      inputs:
        - path: rtsp://localhost:8554/stream  # Socat relay endpoint
          roles: [detect, record]
    detect:
      width: 1280
      height: 720
      fps: 5
